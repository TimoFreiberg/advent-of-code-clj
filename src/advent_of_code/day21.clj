(ns advent-of-code.day21 (:require
                          [advent-of-code.main :refer :all]))

(defn solve-1 [input]
  )

(defn solve-2 [input]
  )

(def input (load-input-file "21"))

(defn solve-day-21 []
  (clojure.pprint/pprint (solve-1 input))
  (clojure.pprint/pprint (solve-2 input)))
